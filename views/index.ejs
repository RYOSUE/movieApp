<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>映画一覧</title>
</head>

<body>
    <h1>映画一覧</h1>
    <a href="/movies/new">新規登録</a>
    <ul>
        <% for (movie of movies) { %>
            <!-- idチェック -->
             <%= movie.id %>
            <h2>
                <%= movie.title %>
            </h2>
            <li>評価: <%= movie.rating %>
            </li>
            <li>観た: <%= movie.watched ? 'はい' : 'いいえ' %>
            </li>
            <hr>
            <% } %>
    </ul>

</body>

</html>